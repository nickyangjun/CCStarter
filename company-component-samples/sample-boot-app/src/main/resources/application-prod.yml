# 正式环境 Profile 示例：spring.profiles.active=prod

component:
  exception:
    enabled: true
    expose-stack-trace: false   # 生产禁止暴露堆栈
  auth:
    enabled: true
    jwt-secret: ${JWT_SECRET}   # 正式环境由运维/配置中心注入
    # 禁止配置 login.test.*（测试验证码仅允许 application-test.yml + profile=test）
