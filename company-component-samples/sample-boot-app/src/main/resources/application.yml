spring:
  application:
    name: sample-boot-app

server:
  port: 18080

management:
  endpoints:
    web:
      exposure:
        include: health,info

# P1+ component.* configuration will be added here when starters are available
component: {}
